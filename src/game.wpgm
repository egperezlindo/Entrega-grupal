import wollok.game.*
import visuales.*
import nivel.*
import musica.*
import menus.*

program wollokDungeons {
    game.width(20)
    game.height(10)
    game.title("Wollok Dungeons")
    game.cellSize(50) 
    game.boardGround("tableroNuevoGame.jpeg")
    menuInicio.abrir()
    game.start()
}